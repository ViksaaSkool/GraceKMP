<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto">

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PICK" />
            <data android:mimeType="image/*" />
        </intent>
    </queries>

    <!-- Permissions: parity with the original Grace manifest -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />

    <!--
      Grace requests contextual / non-personalized ads only, so it does not use the
      Advertising ID. Google Mobile Ads merges its own AD_ID permission in; strip it from
      the merged manifest so Play never sees the app as requesting an ad identifier.
      (Google Play still requires the permission to be *declared* for legacy compatibility
      in some integrations — if ad delivery regresses, revisit this together with the
      Play Console Data Safety answers rather than re-enabling it unilaterally.)
    -->
    <uses-permission
        android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" />

    <uses-feature android:name="android.hardware.camera2.full" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.flash"
        android:required="false" />

    <application
        android:name=".GraceApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/Theme.Grace">

        <!--
          AdMob application ID, injected by Gradle (manifest placeholder). Google's sample
          ID is used in debug; release refuses to build until the production ID is set —
          see docs/monetization-setup.md.
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${adMobAppId}" />

        <!--
          `standard` (the platform default) is stated explicitly because Google Play
          Billing hands the customer off to a banking app for purchase verification; any
          other launch mode can cancel the purchase on return.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="standard"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <!--
              Both keys are required, exactly as in the original Grace manifest.
              androidx.core's FileProvider only looks up the legacy
              `android.support.FILE_PROVIDER_PATHS` key (see
              FileProvider.META_DATA_FILE_PROVIDER_PATHS), while other tooling and
              older/newer consumers read the `androidx.core.*` one. Declaring only
              the androidx key crashes with
              "Missing android.support.FILE_PROVIDER_PATHS meta-data".
            -->
            <meta-data
                android:name="androidx.core.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
